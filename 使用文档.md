# CH9329 自动化控制器 - 使用文档

## 概述

本软件是一款基于 CH9329 芯片的自动化看图控制器，通过串口控制鼠标操作，支持脚本自动化执行和图片预览功能。

---

## 界面布局

### 整体布局

```
┌─────────────────────────────────────────────────────────────┐
│  CH9329 自动化看图控制器                    串口:未连接     │
├──────────────────────────┬──────────────────────────────────┤
│  [串口控制]              │  [图片控制]                     │
│  扫描串口                │  选择文件夹                     │
│  COM下拉框               │  上一张 / 下一张               │
│  连接 / 断开             │  抖动幅度 / 抖动速度            │
├──────────────────────────├──────────────────────────────────┤
│  [脚本控制]              │  [图片预览]                     │
│  脚本下拉框              │                                │
│  编辑 / 执行当前         │        图片显示区域            │
│  新建脚本                │                                │
├──────────────────────────┼──────────────────────────────────┤
│  [自动化控制]            │                                │
│  配置下拉框              │                                │
│  保存 / 编辑 / 新建      │                                │
│  节点列表                │                                │
│  开始自动化 / 停止       │                                │
├──────────────────────────┤                                │
│  [运行日志]              │                                │
│  日志内容显示            │                                │
└──────────────────────────┴──────────────────────────────────┘
```

---

## 功能模块说明

### 1. 串口控制

| 按钮 | 功能 | 操作后果 |
|------|------|----------|
| 扫描串口 | 扫描系统中可用的串口 | 无 |
| COM下拉框 | 选择要连接的串口 | 无 |
| 连接 | 连接选中的串口 | 连接成功后可执行鼠标操作 |
| 断开 | 断开当前串口连接 | 无法执行鼠标操作 |

**操作流程：**
1. 点击「扫描串口」按钮
2. 在下拉框中选择串口（带 CH340 的会自动选中）
3. 点击「连接」按钮

**注意事项：**
- 必须先连接串口才能执行脚本和自动化
- 连接失败请检查串口是否被其他程序占用
- 断开连接后自动化会自动停止

### 2. 脚本控制

| 按钮 | 功能 | 操作后果 |
|------|------|----------|
| 脚本下拉框 | 选择要执行的脚本 | 无 |
| 编辑 | 打开脚本编辑器 | 可修改脚本内容 |
| 执行当前 | 执行选中的脚本 | 通过串口发送鼠标指令 |
| 新建脚本 | 创建新脚本文件 | 打开脚本编辑器 |

**操作流程：**
1. 选择脚本（或新建脚本）
2. 点击「执行当前」

**注意事项：**
- 执行前必须连接串口
- 脚本执行期间无法中断（除非断开串口）

### 3. 图片控制

| 按钮 | 功能 | 操作后果 |
|------|------|----------|
| 选择文件夹 | 选择包含图片的文件夹 | 加载文件夹中的所有图片 |
| 上一张 | 切换到上一张图片 | 无 |
| 下一张 | 切换到下一张图片 | 无 |
| 抖动幅度 | 设置图片抖动幅度 | 影响预览效果 |
| 抖动速度 | 设置图片抖动速度 | 影响预览效果 |

**操作流程：**
1. 点击「选择文件夹」
2. 选择包含图片的文件夹
3. 使用「上一张」/「下一张」切换图片

**支持的图片格式：**
- PNG
- JPG/JPEG
- BMP
- GIF

### 4. 自动化控制

| 按钮 | 功能 | 操作后果 |
|------|------|----------|
| 配置下拉框 | 选择自动化配置 | 无 |
| 保存 | 保存当前配置 | 覆盖现有配置文件 |
| 编辑 | 编辑当前配置 | 打开配置编辑器 |
| 新建 | 创建新配置 | 打开配置编辑器 |
| 开始自动化 | 启动自动化流程 | 按配置执行所有节点 |
| 停止 | 停止自动化流程 | 立即停止执行 |

**操作流程：**
1. 选择或创建配置
2. 连接串口
3. 点击「开始自动化」

**注意事项：**
- 自动化会循环执行直到点击「停止」
- 执行期间可以随时点击「停止」中断

### 5. 运行日志

显示程序运行日志，包括：
- 系统启动信息
- 串口连接状态
- 脚本执行结果
- 自动化进度
- 错误信息

---

## 脚本编写

### 创建脚本

1. 点击「新建脚本」按钮
2. 输入脚本名称（自动添加 `.txt` 后缀）
3. 在编辑区编写脚本内容
4. 点击「保存」

### 脚本格式

```txt
# 这是注释行（以 # 开头）
(100, 200, 1, 500)   # (X坐标, Y坐标, 按键类型, 延迟毫秒)
(300, 400, 2, 800)
```

### 指令格式

每条指令为一个四元组：`(x, y, key, delay_ms)`

| 参数 | 说明 | 取值范围 |
|------|------|----------|
| x | 目标X坐标 | 正整数 |
| y | 目标Y坐标 | 正整数 |
| key | 按键类型 | 0=无, 1=左键, 2=右键, 3=双击 |
| delay_ms | 执行后延迟时间 | 正整数（毫秒） |

### 按键类型说明

| 值 | 按键 | 说明 |
|----|------|------|
| 0 | 无 | 只移动不点击 |
| 1 | 左键 | 移动到位置后左键单击 |
| 2 | 右键 | 移动到位置后右键单击 |
| 3 | 双击 | 移动到位置后双击 |

### 脚本示例

```txt
# 扫码脚本示例
# 移动到二维码位置并点击
(150, 200, 1, 500)   # 移动到二维码位置，左键点击，等待500ms

# 等待扫码完成
(0, 0, 0, 2000)      # 等待2秒

# 移动到确认按钮
(400, 300, 1, 500)   # 点击确认按钮
```

### 执行流程

1. 鼠标先归位到原点
2. 移动到目标X坐标
3. 移动到目标Y坐标
4. **固定等待500ms**（确保鼠标稳定）
5. 执行点击操作（如果有）
6. 等待指定的延迟时间

---

## 自动化配置

### 创建配置

1. 点击「新建」按钮
2. 输入配置名称（自动添加 `.json` 后缀）
3. 添加自动化节点
4. 设置显示参数和循环选项
5. 点击「保存」

### 节点类型

| 类型 | 说明 | 配置参数 |
|------|------|----------|
| 脚本 | 执行脚本内容 | 脚本名称、脚本内容、执行后延迟 |
| 延时 | 固定等待时间 | 延时毫秒数 |
| 图片切换 | 切换到上一张/下一张 | 方向（next/prev） |
| 图片跳转 | 跳转到指定图片 | 图片索引 |

### 添加节点步骤

1. 在配置编辑器中点击「+ 添加脚本」、「+ 延时」或「+ 图片切换」
2. 配置节点参数
3. 使用「↑上移」「↓下移」调整顺序
4. 使用「删除」移除节点

### 配置示例

```json
{
  "name": "扫码流程",
  "nodes": [
    {"type": "script", "name": "扫描二维码", "content": "(100,200,1,500)", "delay_after_ms": 500},
    {"type": "delay", "duration_ms": 2000},
    {"type": "image_switch", "direction": "next"},
    {"type": "delay", "duration_ms": 1000}
  ],
  "image_display": {"shake_amplitude": 10, "shake_speed": 0.2},
  "loop": {"enabled": true, "count": 0}
}
```

### 显示设置

| 参数 | 说明 | 默认值 |
|------|------|--------|
| 抖动幅度 | 图片抖动的最大偏移量 | 10 |
| 抖动速度 | 图片抖动的频率 | 0.2 |

### 循环设置

| 参数 | 说明 | 取值 |
|------|------|------|
| 启用循环 | 是否循环执行 | 是/否 |
| 循环次数 | 执行次数（0=无限） | 整数 |

---

## 完整操作流程

### 基本使用流程

1. **启动程序**：双击「启动脚本.bat」或运行 `python main_ui.py`

2. **连接串口**：
   - 点击「扫描串口」
   - 选择串口（CH340会自动选中）
   - 点击「连接」

3. **加载图片**：
   - 点击「选择文件夹」
   - 选择图片文件夹

4. **执行脚本**：
   - 选择脚本
   - 点击「执行当前」

5. **运行自动化**：
   - 选择或创建配置
   - 点击「开始自动化」
   - 点击「停止」结束

### 高级使用流程

1. **创建自定义脚本**：
   - 点击「新建脚本」
   - 编写脚本内容
   - 保存

2. **创建自动化配置**：
   - 点击「新建」配置
   - 添加节点（脚本、延时、图片切换）
   - 设置循环次数
   - 保存配置

3. **运行自动化**：
   - 选择配置
   - 点击「开始自动化」

---

## 常见问题

### Q1: 串口连接失败

**可能原因：**
- 串口被其他程序占用
- 串口不存在或已损坏
- CH340驱动未安装

**解决方案：**
- 关闭其他使用串口的程序
- 重新插拔USB设备
- 安装CH340驱动

### Q2: 脚本执行无反应

**可能原因：**
- 串口未连接
- 脚本格式错误
- CH9329设备未正确连接

**解决方案：**
- 检查串口连接状态
- 查看日志错误信息
- 检查设备连接

### Q3: 图片无法显示

**可能原因：**
- 文件夹中无图片文件
- 图片格式不支持
- 图片文件损坏

**解决方案：**
- 选择包含有效图片的文件夹
- 使用支持的格式（PNG/JPG/BMP/GIF）
- 检查图片文件

### Q4: 自动化无法循环

**可能原因：**
- 循环设置未启用
- 已达到设定的循环次数

**解决方案：**
- 在配置编辑器中启用循环
- 设置循环次数为0（无限循环）

---

## 快捷键

目前版本暂未支持快捷键，所有操作通过鼠标点击完成。

---

## 版本信息

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0 | 2026-06-04 | 初始版本 |