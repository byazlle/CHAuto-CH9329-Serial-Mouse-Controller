# CH9329 自动化控制器 - 开发文档

## 项目结构

```
项目根目录/
├── automation.py          # 自动化流程引擎
├── config_manager.py      # 配置文件管理
├── connection_handler.py  # 连接模块（协调各模块）
├── image_manager.py       # 图片管理模块
├── main_ui.py             # 主界面模块
├── mouse_command.py       # 鼠标指令转换模块
├── script_manager.py      # 脚本文件管理
├── serial_control.py      # 串口控制模块
├── configs/               # 自动化配置目录
│   └── 默认配置.json
├── scripts/               # 脚本文件目录
│   └── 默认扫码脚本.txt
└── 启动脚本.bat           # 启动脚本
```

## 模块职责

| 模块 | 职责 | 文件 |
|------|------|------|
| **主界面** | UI渲染、按钮事件、状态显示 | main_ui.py |
| **连接处理** | 协调各模块、回调管理 | connection_handler.py |
| **串口控制** | 串口连接、数据读写 | serial_control.py |
| **鼠标指令** | CH9329协议转换、指令发送 | mouse_command.py |
| **图片管理** | 图片加载、切换、抖动效果 | image_manager.py |
| **脚本管理** | 脚本文件读写、解析 | script_manager.py |
| **自动化引擎** | 节点执行、循环控制 | automation.py |
| **配置管理** | 配置文件读写、验证 | config_manager.py |

---

## 1. 串口控制模块 (serial_control.py)

### 类：SerialControl

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `connect(port, baud=9600)` | port: str, baud: int | (bool, str) | 连接串口 |
| `disconnect()` | 无 | (bool, str) | 断开连接 |
| `write(data)` | data: bytes | bool | 写入数据 |
| `read(size=1024)` | size: int | bytes | 读取数据 |

#### 属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `is_connected` | bool | 是否已连接 |
| `connected_port` | str | 当前连接的端口名 |

---

## 2. 鼠标指令模块 (mouse_command.py)

### 类：MouseCommand

#### 协议配置

| 常量 | 值 | 说明 |
|------|-----|------|
| HEAD | [0x57, 0xAB] | 协议头 |
| ADDR | 0x00 | 设备地址 |
| CMD_MOUSE | 0x05 | 鼠标命令 |
| DATA_LEN | 0x05 | 数据长度 |
| KEY_NONE | 0x00 | 无按键 |
| KEY_LEFT | 0x01 | 左键 |
| KEY_RIGHT | 0x02 | 右键 |
| KEY_BOTH | 0x03 | 双键 |

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `send_mouse(x_rel, y_rel, key=0, wait_ms=1)` | x_rel: int, y_rel: int, key: int, wait_ms: int | bool | 发送相对移动指令 |
| `move_to(x, y, key=0, delay_ms=500)` | x: int, y: int, key: int, delay_ms: int | bool | 移动到绝对坐标 |
| `run_script(script, progress_callback=None)` | script: list, progress_callback: callable | bool | 执行脚本序列 |
| `stop()` | 无 | None | 停止当前操作 |

#### 指令格式

每条鼠标指令为四元组：`(x, y, key, delay_ms)`

| 参数 | 类型 | 范围 | 说明 |
|------|------|------|------|
| x | int | 正整数 | 目标X坐标 |
| y | int | 正整数 | 目标Y坐标 |
| key | int | 0-3 | 按键类型 |
| delay_ms | int | >=0 | 执行后延迟(ms) |

---

## 3. 脚本管理模块 (script_manager.py)

### 类：ScriptManager

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `list_scripts()` | 无 | List[str] | 列出所有脚本文件名 |
| `load_script(filename)` | filename: str | (bool, str, list) | 加载并解析脚本 |
| `load_script_content(filename)` | filename: str | (bool, str, str) | 加载原始内容 |
| `save_script(filename, content, validate=True)` | filename: str, content: str, validate: bool | (bool, str) | 保存脚本 |
| `delete_script(filename)` | filename: str | (bool, str) | 删除脚本 |

#### 脚本文件格式

```txt
# 注释行（以#开头）
(100, 200, 1, 500)   # (x, y, key, delay_ms)
(300, 400, 2, 800)   # 右键点击后等待800ms
```

---

## 4. 配置管理模块 (config_manager.py)

### 类：ConfigManager

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `list_configs()` | 无 | List[str] | 列出所有配置文件名 |
| `load_config(filename)` | filename: str | (bool, str, dict) | 加载配置 |
| `save_config(filename, data)` | filename: str, data: dict | (bool, str) | 保存配置 |
| `delete_config(filename)` | filename: str | (bool, str) | 删除配置 |
| `create_default_config()` | 无 | dict | 创建默认配置 |

#### 配置文件格式 (JSON)

```json
{
  "name": "配置名称",
  "version": "1.0",
  "created_at": "2026-06-04",
  "nodes": [
    {"type": "script", "name": "脚本名", "content": "...", "delay_after_ms": 500},
    {"type": "delay", "duration_ms": 2000},
    {"type": "image_switch", "direction": "next"}
  ],
  "image_display": {
    "shake_amplitude": 10,
    "shake_speed": 0.2
  },
  "loop": {
    "enabled": true,
    "count": 0
  }
}
```

---

## 5. 自动化引擎模块 (automation.py)

### 类：AutomationEngine

#### 节点类型

| 类型 | 参数 | 说明 |
|------|------|------|
| `script` | name, content, delay_after_ms | 执行脚本 |
| `delay` | duration_ms | 固定延时 |
| `image_switch` | direction (next/prev) | 切换图片 |
| `image_goto` | index | 跳转到指定图片 |
| `mouse_move` | x, y, key, delay_ms | 单次鼠标操作 |

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `run(config_data=None)` | config_data: dict | None | 启动自动化 |
| `stop()` | 无 | None | 停止自动化 |
| `load_config(config_data)` | config_data: dict | bool | 加载配置 |
| `set_callbacks(on_progress, on_status_change, on_log)` | callables | None | 设置回调 |

---

## 6. 连接处理模块 (connection_handler.py)

### 类：ConnectionHandler

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `scan_serial_ports()` | 无 | (bool, str, list, str) | 扫描串口 |
| `connect_serial(port)` | port: str | (bool, str) | 连接串口 |
| `disconnect_serial()` | 无 | (bool, str) | 断开串口 |
| `run_single_script(filename)` | filename: str | (bool, str) | 执行单个脚本 |
| `run_script_from_content(content)` | content: str | (bool, str) | 从内容执行脚本 |
| `start_automation(config_data)` | config_data: dict | (bool, str) | 启动自动化 |
| `stop_automation()` | 无 | (bool, str) | 停止自动化 |

#### 回调接口

```python
set_callbacks(
    on_status_update: Callable[[str], None],      # 状态更新回调
    on_com_update: Callable[[str], None],         # 串口状态回调
    on_log_message: Callable[[str], None],        # 日志消息回调
    on_error: Callable[[str, str], None]          # 错误回调
)
```

---

## 7. 图片管理模块 (image_manager.py)

### 类：ImageManager

#### 属性

| 属性 | 类型 | 说明 |
|------|------|------|
| `SHAKE_AMPLITUDE` | float | 抖动幅度（默认10） |
| `FLOAT_SPEED` | float | 抖动速度（默认0.2） |
| `image_list` | list | 加载的图片路径列表 |

#### 方法

| 方法 | 参数 | 返回值 | 说明 |
|------|------|--------|------|
| `load_folder(folder_path)` | folder_path: str | (bool, str, int) | 加载文件夹 |
| `next()` | 无 | None | 下一张 |
| `prev()` | 无 | None | 上一张 |
| `go_to(index)` | index: int | None | 跳转到指定索引 |
| `get_current_image_info()` | 无 | dict | 获取当前图片信息 |
| `render_for_canvas(width, height, apply_effect=False)` | width, height, apply_effect | tuple | 渲染图片 |

---

## 代码编写规范

### 1. 文件结构规范

- 每个模块独立一个 `.py` 文件
- 类名使用 PascalCase
- 方法名和变量名使用 snake_case
- 常量使用 UPPER_CASE

### 2. 返回值规范

所有公共方法应返回 `(success: bool, message: str, [data])` 格式

### 3. 线程安全规范

- 使用 `threading.Lock()` 保护共享资源
- UI更新必须通过 `root.after()` 在主线程执行
- 长时间操作应在后台线程执行

### 4. 错误处理规范

- 使用 try-except 捕获异常
- 记录错误日志
- 通过回调通知上层

### 5. 单例模式规范

```python
_instance = None
_lock = threading.Lock()

def get_instance():
    global _instance
    with _lock:
        if _instance is None:
            _instance = ClassName()
    return _instance
```

---

## 协议说明

### CH9329 鼠标指令协议

```
数据包格式（11字节）：
[0x57, 0xAB, ADDR, CMD, LEN, RID, KEY, X, Y, WHEEL, CHECKSUM]

HEAD:      0x57, 0xAB  (固定)
ADDR:      0x00       (设备地址)
CMD:       0x05       (鼠标命令)
LEN:       0x05       (数据长度)
RID:       0x01       (报告ID)
KEY:       0x00-0x03  (按键状态)
X/Y:       0x00-0xFF  (相对坐标)
WHEEL:     0x00       (滚轮)
CHECKSUM:  累加校验和
```

### 校验和算法

```python
checksum = sum(data) & 0xFF
```

---

## 数据文件

### scripts/ 目录

存放 `.txt` 格式的脚本文件

### configs/ 目录

存放 `.json` 格式的配置文件

---

## 版本历史

| 版本 | 日期 | 变更 |
|------|------|------|
| 1.0 | 2026-06-04 | 初始版本 |